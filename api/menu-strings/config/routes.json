{
  "routes": [
    {
      "method": "GET",
      "path": "/menu-strings",
      "handler": "menu-strings.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/menu-strings",
      "handler": "menu-strings.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/menu-strings",
      "handler": "menu-strings.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
